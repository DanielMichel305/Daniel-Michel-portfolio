<div>

    <h3>Blogs</h3>
    <% blogs.forEach(blog => { %>
        
        <hr>
        <h3><%= blog.blogTitle %></h3>
        <h5><%= blog.blog_id %></h5> <h6><%= blog.createdAt %></h6>
        <p><%= blog.blogDescription %></p>
        <hr>

    <% }) %>
    

</div>